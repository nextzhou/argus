version: v0.1.0
id: argus-project-setup
order: 10
description: "Project-level Argus setup exists"
auto: session_start

check:
  - shell: "test -d .argus"
    description: "Project-level Argus directory exists"

prompt: |
  Set up project-level Argus in this repository with `argus setup --yes`. Use the `argus-setup` skill if guided setup is more appropriate.
workflow: ""
